# Session 2026-05-20 — β 上线大冲刺日报

> 工作时段：2026-05-20 09:00 HKT → 2026-05-21 04:00 HKT（连续 19 小时）
> 协作模式：1 CEO（Cowork）+ 4 部门（warp Claude Code CLI tab）
> 总计：154 commits / 12.5K LOC / 8+ tickets / ~$25-40 token cost

---

## 战果

### Backend
- **Backend 064** — migration 049 `data_health_history` 表 + snapshot cron 真 INSERT
- **Backend 065** — `beta-readiness-check` edge function（7-check 一键自检 + runbook）
- **Backend 066** — `wechat-jssdk-signature` Supabase edge function（首版，因 IP pool 漂移 deprecate）
- **Backend 067** — 真签 verify 跑 3 次确认 IP pool 每次不同 → escalate Railway 方案
- **Backend 068** — `wechat-jssdk-signature` 搬到 Railway `server.js`（in-memory cache + sha1 sig + `/api/_egress-ip` helper）

### UI
- **UI 063** — 头像上传 (canvas 256×256) + 家庭成员卡 + 节庆 toast
- **UI 067** — PII 收口 (jianjiaolin9 → support@nothinkeats.com) + 删 Settings ProToolbox + 小美料理机并入家政区
- **UI 068** — 应季 chip + 菲佣邀请码登录 + Login 删 IG/FB + Settings WhatsApp 一键分享 + BETA 文案双套 + index.html OG meta tags + og-image.png v3 (cream + 大叶子)
- **UI 069** — og-image v3 push (mockup-style)
- **UI 070** — Home 语言 picker 按 role 3 种 + `?fresh=1` 强制看 Login
- **UI 075**（8 件合一）：
  - 删 Home 营养雷达（移到 /weekly）
  - 删 Settings 语言切换卡（首页已有）
  - 删 Login IG/FB 按钮
  - /verify 删强制 Pro 跳转 + 一键采购/导出按钮级 paywall
  - /weekly freemium day window（今+明 免费 / 后续 Pro blur preview）
  - /weekly 营养雷达 Pro gate + RadarPreview placeholder
  - Settings MembershipCard 精简为 4 状态状态卡（删大块橙色推广）
  - QuickSetup 目标 + 家乡 改多选

### Algorithm
- **Algorithm 064** — axis 31 fruit/veggie 应季强化 (+0.40/-0.20) + bump ALGO_VERSION v44 + 4 节气 simulation
- **Algorithm 068** — export `isFruitVeggieInSeason` + `getCurrentSolarTermZh` helpers（non-algorithmic export，UI 调用即可）

### Database
- **Database 065** — migration 047 `ingredient_seasonality` 表 + 048 backfill 63 行
- **Database 066** — migration 050 `user_weekly_menus.algo_version` legacy backfill 337 行（Smell 4 完全收口）

### CEO 端
- **Memory 立法 3 条**（永久铁律）：
  - `feedback_isolate_protect_personal_info` — 私人邮箱/微信/真名/手机/住址绝不暴露
  - `feedback_no_cash_rewards_ever` — 任何裂变机制禁止 PayMe/现金/红包
  - `feedback_daily_token_cost_report` — 每 ticket + 每日 token 汇报
- **3 docs PII 清理**（CEO 直接 Edit）：OAUTH_SETUP.md / docs/PITCH_03_mvp_terms.md / docs/BETA_LAUNCH_COPY.md
- **Cloudflare Email Routing 配置指引** + Gmail filter 双层防灌爆方案
- **canvas-design og-image.png v3 生成**（Pillow + cream/leaf hero）

---

## 阻塞 / 待续作（明早第一时间）

### P0 — 微信 wx-jssdk 通路（卡在 Railway 平台事故）

凌晨 3:55 HKT Railway 报：「Builds are slow to progress. We have pushed a fix and are now monitoring the incident.」

**老板已做**：
- 微信公众号 IP 白名单已加 18.144.156.191（旧 Supabase IP，可保留也可删）
- 公众号后台 3 个域名（业务/JS 接口/网页授权）已配 `nothinkeats.com`
- Railway 上 `WECHAT_APPID` + `WECHAT_APPSECRET` 已灌（新 AppID `wx63839880f1595f07`）
- Supabase secrets 也同步更新

**明早 Railway 恢复后 3 步收尾**（10 分钟）：
1. 等 Railway redeploy 自动跑完（或手动 Redeploy）
2. `curl https://nothinkeats.com/api/_egress-ip` 拿 Railway 出口 IP
3. 把 IP 加进微信白名单 → `curl POST /api/wechat-jssdk-signature` 拿 signature ✅

成功 signature 后 → 派 UI 076 接入 `useWeChatShare` hook（10 分钟）→ 微信群分享显示 hero 卡。

### P1 — Algorithm 069 / Database 067

- **Algorithm 069**：`getNutritionGapRecommendations(deficits, n)` helper — 给营养雷达 Pro 卡推荐补全菜（25 分钟）
- **Database 067**：Smell 3 B-1 完整收口（household_members.helper_id text 迁移 + FK→user_profiles + 5 RLS anon-first）（30 分钟，菲佣 onboarding 依赖）

### P2 — Railway Reserved IP 付费上锁（永久解 IP 漂移）

Railway dashboard → Project Settings → Networking → Reserved IP（~USD$5/月）→ 永久 IP → 微信白名单一次配死。

---

## 当前可分享状态

| 渠道 | 链接预览 | 状态 |
|---|---|---|
| WhatsApp | ✅ Hero 卡（橙底大叶子）| ship |
| 朋友圈 | ✅ Hero 卡 | ship |
| Twitter / FB / LinkedIn | ✅ Hero 卡 | ship |
| 微信群 | ⏸ 仅 title + description（明早 Railway 恢复后 ship hero 卡）| 待续 |

**β 邀请链**：`https://nothinkeats.com`（可现在发；微信群预览明早 upgrade）
**测试入口**：`https://nothinkeats.com/login`（直接进 Login 不走 onboarding）
**强制重置**：`https://nothinkeats.com/?fresh=1`（清登录态进 Login）
**OG 真签验证**：`curl https://nothinkeats.com/api/wechat-jssdk-signature -X POST -d '{"url":"https://nothinkeats.com/"}'`

---

## 关键洞察 / 决策

1. **freemium with progressive disclosure** — 看得到 Pro 功能存在产生欲望，付费才能用（雷达 / 周菜单后续 / 一键采购）
2. **菲佣裂变 3 层架构** — L1 中介合作（暂不做）/ L2 菲佣自邀请（β 后期做）/ L3 雇主互邀（下周 Stripe coupon）
3. **永不现金奖励** — 用积分 + 视频培训 + 社区 + 超市券（超市供货）替代
4. **PII 全面隔离** — support@nothinkeats.com + Cloudflare Email Routing + Gmail filter 三层防护
5. **Anonymous-first onboarding 流** — 访客不需登录即走 /setup 问卷（降低注册门槛），/login 仅可选附加社交账号

---

## 明早开工流程（醒来后给未来的我）

1. 读这份 SESSION-2026-05-20.md
2. 读 _bridge/telepot_*.md 看 4 部门当前状态
3. 检查 Railway 是否恢复（看 Deployments 最新状态 + status.railway.com）
4. 若恢复 → 跑 P0 微信收尾 3 步
5. 派 P1 Algorithm 069 + Database 067 + UI 076
6. 等老板早起来验真签 + 后续指令
