[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["NODE_ENV=development npm install --production=false"]
cacheDirectories = ["node_modules"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
