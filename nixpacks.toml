[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --prefer-offline"]
cacheDirectories = ["node_modules"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server.js"
